# Car 360 — auto rotabile con pallini dei servizi — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Sostituire l'immagine 2D dello spaccato nella sezione "Esplora i servizi" con un'auto (Audi RS Q8 nera) rotabile via trascinamento, con i pallini dei servizi ancorati ai punti dell'auto.

**Architecture:** Nessuna libreria 3D. Una sequenza di 36 fotogrammi WebP pre-renderizzati (arco ~180°) mostrati in base al trascinamento. Un nuovo componente client `Car360` sostituisce `CarStage` dentro `CarExplorer`, riusando `ServicePanel` e `ZoneNav`. La geometria dei pallini è definita per campioni interpolati lungo la rotazione, in modo agnostico rispetto a numero di frame e ampiezza dell'arco (estendibile a 360° senza toccare il codice).

**Tech Stack:** Next 16, React 19, TypeScript, Tailwind v4, framer-motion (già presenti). Vitest (nuovo, solo per la logica pura di `carSpin.ts`). Higgsfield MCP + ffmpeg per la produzione degli asset.

## Global Constraints

- Testi UI in **italiano**, con accenti corretti.
- Riusare i componenti esistenti `ServicePanel` e `ZoneNav`; non duplicarli.
- Gli id dei servizi restano quelli di `treatments.ts`; i link puntano a `/trattamenti#<id>`.
- L'auto è **immobile da ferma** (nessuna auto-rotazione): si muove solo al drag.
- Sistema **agnostico** a `frameCount` e `arcDegrees` (estendibile a 360° = solo asset + campioni in più).
- 36 fotogrammi per l'arco di 180°.
- Asset in `public/home/spin/frame-000.webp … frame-035.webp`.
- Nessuna nuova dipendenza runtime pesante (solo asset immagine + codice).

---

## Nota sull'esecuzione degli asset (Task 1–3)

I Task 1–3 producono immagini tramite Higgsfield MCP e ffmpeg e **non sono TDD**: la verifica è visiva (ispezione dei fotogrammi). Vanno eseguiti dall'agente che dispone dei tool MCP Higgsfield (l'agente principale della sessione). I Task 4–10 sono codice e seguono TDD/verifica visiva.

L'immagine sorgente pulita di partenza è derivata da `vetri_e_fari.png` (Audi RS Q8 nera, fondo scuro), da cui vanno rimossi la scritta "Audi Sport" sulla portiera e lo sparkle in basso a destra.

---

### Task 1: Base pulita dell'Audi nera

**Files:**
- Create: `public/home/spin/_base.png` (plate sorgente pulito, non spedito al client)

**Interfaces:**
- Produces: un'immagine front-3/4 dell'Audi RS Q8 nera su fondo scuro coerente con la sezione (`bg-ink`), **senza** testo sulla portiera e **senza** sparkle.

- [ ] **Step 1: Caricare la sorgente su Higgsfield**

Upload di `vetri_e_fari.png` con `media_upload` → `media_confirm`. Annota il `media_id`.

- [ ] **Step 2: Generare/pulire il plate**

Con `generate_image` (modello raccomandato via `models_explore(action:'recommend', type:'image')` per "clean up product photo, remove text and sparkle overlay, keep identical black Audi RS Q8 on dark studio background"), passando il `media_id` come riferimento. Prompt: rimuovere la scritta "Audi Sport" dalla portiera e lo sparkle in basso a destra, mantenere identica l'auto e l'illuminazione, fondo studio scuro uniforme.

- [ ] **Step 3: Verifica visiva**

Scaricare il risultato in `public/home/spin/_base.png` e ispezionarlo (Read dell'immagine): auto integra, niente testo/sparkle, fondo scuro pulito. Se non soddisfacente, rigenerare variando il prompt prima di procedere.

- [ ] **Step 4: Commit**

```bash
git add public/home/spin/_base.png
git commit -m "feat(car-360): plate base pulito dell'Audi nera"
```

---

### Task 2: Orbita 180° (clip concatenate)

**Files:**
- Create: `public/home/spin/_orbit.mp4` (video sorgente, non spedito al client)

**Interfaces:**
- Consumes: `_base.png` come start frame.
- Produces: un video che copre ~180° (front-3/4 → fiancata → rear-3/4), illuminazione e fondo coerenti.

- [ ] **Step 1: Prima clip**

Upload di `_base.png`. Con `generate_video` (modello `kling3_0_turbo`, `sound:"off"`, `aspect_ratio:"16:9"`, `duration:5`) e prompt di orbita lenta e costante attorno all'auto ferma, generare la prima clip (~parte dell'arco). Annota il `job_id`.

- [ ] **Step 2: Clip successive concatenate**

Estrarre l'ultimo fotogramma della clip precedente (vedi tecnica Task 3) e usarlo come `start_image` della clip successiva. Ripetere finché la somma delle clip copre ~180°. Mantenere lo stesso prompt/illuminazione per continuità.

- [ ] **Step 3: Verifica continuità**

Ispezionare i punti di giunzione (ultimo frame di una clip vs primo della successiva): l'auto deve restare integra e l'illuminazione coerente. Rigenerare le clip stonate.

- [ ] **Step 4: Concatenare in un unico mp4**

```bash
# elenco clip in ordine in concat.txt, poi:
ffmpeg -f concat -safe 0 -i concat.txt -c copy public/home/spin/_orbit.mp4
```

- [ ] **Step 5: Commit**

```bash
git add public/home/spin/_orbit.mp4
git commit -m "feat(car-360): orbita 180 concatenata"
```

---

### Task 3: Estrazione e ottimizzazione dei 36 fotogrammi

**Files:**
- Create: `public/home/spin/frame-000.webp … frame-035.webp`
- Delete (a fine task): `public/home/spin/_base.png`, `public/home/spin/_orbit.mp4`

**Interfaces:**
- Produces: 36 fotogrammi WebP equidistanti lungo l'arco, larghezza ~1280px, fondo scuro coerente.

- [ ] **Step 1: Assicurarsi che ffmpeg sia disponibile**

Se `ffmpeg` non è nel PATH: `scoop install ffmpeg` (l'utente ha scoop). Verifica: `ffmpeg -version`.

- [ ] **Step 2: Estrarre 36 fotogrammi equidistanti**

```bash
cd public/home/spin
# calcola la durata e campiona 36 frame uniformi:
ffmpeg -i _orbit.mp4 -vf "scale=1280:-1,fps=36/$(ffprobe -v error -show_entries format=duration -of csv=p=0 _orbit.mp4)" -frames:v 36 tmp-%03d.png
```

Se il conteggio non è esattamente 36, aggiustare l'espressione `fps` o usare `select` per ottenere 36 PNG `tmp-001…tmp-036`.

- [ ] **Step 3: Convertire in WebP numerati da 000**

```bash
i=0; for f in $(ls tmp-*.png | sort); do
  printf -v n "%03d" $i
  ffmpeg -i "$f" -q:v 82 "frame-$n.webp"
  i=$((i+1))
done
rm tmp-*.png
```

- [ ] **Step 4: Verifica visiva**

Read di `frame-000.webp`, `frame-017.webp`, `frame-035.webp`: rotazione progressiva, auto integra, nessun testo/sparkle, fondo coerente. Controllare il peso totale (`ls -la`): la sequenza deve restare contenuta (obiettivo indicativo < ~3 MB totali; se troppo pesante abbassare `-q:v`).

- [ ] **Step 5: Rimuovere i sorgenti e committare**

```bash
rm -f _base.png _orbit.mp4
git add public/home/spin
git commit -m "feat(car-360): 36 fotogrammi WebP dell'orbita 180"
```

---

### Task 4: Setup Vitest per la logica pura

**Files:**
- Modify: `package.json` (script `test`, devDeps)
- Create: `vitest.config.ts`

**Interfaces:**
- Produces: comando `npm test` che esegue Vitest in ambiente node.

- [ ] **Step 1: Installare Vitest**

```bash
npm i -D vitest
```

- [ ] **Step 2: Config Vitest**

Create `vitest.config.ts`:

```ts
import { defineConfig } from "vitest/config";
import { resolve } from "node:path";

export default defineConfig({
  test: { environment: "node", include: ["src/**/*.test.ts"] },
  resolve: { alias: { "@": resolve(__dirname, "src") } },
});
```

- [ ] **Step 3: Script test in package.json**

Aggiungere a `"scripts"`: `"test": "vitest run"`.

- [ ] **Step 4: Verifica**

Run: `npm test`
Expected: Vitest parte e riporta "No test files found" (o 0 test) senza errori di config.

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json vitest.config.ts
git commit -m "chore(test): setup vitest per la logica pura"
```

---

### Task 5: `carSpin.ts` — config sequenza e funzioni pure

**Files:**
- Create: `src/lib/carSpin.ts`
- Test: `src/lib/carSpin.test.ts`

**Interfaces:**
- Produces:
  - `SPIN: { frameCount: number; arcDegrees: number; wrap: boolean; srcFor(i: number): string }`
  - `normalizeFrame(frame: number, count?: number, wrap?: boolean): number`
  - `frameIndex(frame: number, count?: number, wrap?: boolean): number`
  - `type SpotSample = { frame: number; x: number; y: number; visible: boolean }`
  - `type ResolvedSpot = { x: number; y: number; opacity: number }`
  - `resolveSpot(samples: SpotSample[], frame: number): ResolvedSpot`

- [ ] **Step 1: Scrivere i test (falliscono)**

Create `src/lib/carSpin.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { SPIN, normalizeFrame, frameIndex, resolveSpot } from "./carSpin";

describe("SPIN config", () => {
  it("ha 36 frame e arco 180 non-wrap", () => {
    expect(SPIN.frameCount).toBe(36);
    expect(SPIN.arcDegrees).toBe(180);
    expect(SPIN.wrap).toBe(false);
  });
  it("srcFor pad a 3 cifre", () => {
    expect(SPIN.srcFor(0)).toBe("/home/spin/frame-000.webp");
    expect(SPIN.srcFor(35)).toBe("/home/spin/frame-035.webp");
  });
});

describe("normalizeFrame", () => {
  it("clampa fuori range quando wrap=false", () => {
    expect(normalizeFrame(-3, 36, false)).toBe(0);
    expect(normalizeFrame(40, 36, false)).toBe(35);
    expect(normalizeFrame(12.4, 36, false)).toBeCloseTo(12.4);
  });
  it("wrappa quando wrap=true", () => {
    expect(normalizeFrame(-1, 36, true)).toBe(35);
    expect(normalizeFrame(36, 36, true)).toBe(0);
  });
});

describe("frameIndex", () => {
  it("arrotonda all'intero più vicino", () => {
    expect(frameIndex(12.4, 36, false)).toBe(12);
    expect(frameIndex(12.6, 36, false)).toBe(13);
  });
});

describe("resolveSpot", () => {
  const samples = [
    { frame: 0, x: 10, y: 50, visible: true },
    { frame: 10, x: 30, y: 50, visible: true },
    { frame: 20, x: 50, y: 50, visible: false },
  ];
  it("interpola x/y tra due campioni", () => {
    const r = resolveSpot(samples, 5);
    expect(r.x).toBeCloseTo(20);
    expect(r.opacity).toBeCloseTo(1);
  });
  it("sfuma l'opacità verso un campione non visibile", () => {
    const r = resolveSpot(samples, 15);
    expect(r.x).toBeCloseTo(40);
    expect(r.opacity).toBeCloseTo(0.5);
  });
  it("usa gli estremi fuori range", () => {
    expect(resolveSpot(samples, -5).x).toBeCloseTo(10);
    expect(resolveSpot(samples, 99).opacity).toBeCloseTo(0);
  });
});
```

- [ ] **Step 2: Eseguire i test (falliscono)**

Run: `npm test`
Expected: FAIL — `carSpin` non esiste / export mancanti.

- [ ] **Step 3: Implementare `carSpin.ts`**

Create `src/lib/carSpin.ts`:

```ts
/**
 * Configurazione della sequenza di rotazione dell'auto (sezione CarExplorer) e
 * funzioni pure per mappare la posizione di trascinamento sul fotogramma e per
 * risolvere la posizione/opacità dei pallini lungo la rotazione.
 *
 * Tutto è agnostico rispetto a `frameCount` e `arcDegrees`: per passare da 180°
 * a 360° basta rigenerare più fotogrammi (alzare frameCount), mettere wrap=true
 * e aggiungere campioni ai pallini. Il resto del codice non cambia.
 */
export const SPIN = {
  frameCount: 36,
  arcDegrees: 180,
  wrap: false,
  srcFor: (i: number) => `/home/spin/frame-${String(i).padStart(3, "0")}.webp`,
};

/** Riporta un frame (anche frazionario) nel range valido: clamp o wrap. */
export function normalizeFrame(
  frame: number,
  count: number = SPIN.frameCount,
  wrap: boolean = SPIN.wrap,
): number {
  if (wrap) return ((frame % count) + count) % count;
  return Math.max(0, Math.min(count - 1, frame));
}

/** Indice intero del fotogramma da mostrare. */
export function frameIndex(
  frame: number,
  count: number = SPIN.frameCount,
  wrap: boolean = SPIN.wrap,
): number {
  return Math.round(normalizeFrame(frame, count, wrap)) % count;
}

export type SpotSample = { frame: number; x: number; y: number; visible: boolean };
export type ResolvedSpot = { x: number; y: number; opacity: number };

const lerp = (a: number, b: number, t: number) => a + (b - a) * t;

/**
 * Posizione (x/y in %) e opacità di un pallino a un dato frame frazionario.
 * I `samples` devono essere ordinati per `frame` crescente. Fuori dagli estremi
 * si usano il primo/ultimo campione. `visible` è trattato come 1/0 e interpolato
 * per ottenere una dissolvenza morbida.
 */
export function resolveSpot(samples: SpotSample[], frame: number): ResolvedSpot {
  if (samples.length === 0) return { x: 0, y: 0, opacity: 0 };
  const first = samples[0];
  const last = samples[samples.length - 1];
  if (frame <= first.frame) return { x: first.x, y: first.y, opacity: first.visible ? 1 : 0 };
  if (frame >= last.frame) return { x: last.x, y: last.y, opacity: last.visible ? 1 : 0 };

  for (let i = 0; i < samples.length - 1; i++) {
    const a = samples[i];
    const b = samples[i + 1];
    if (frame >= a.frame && frame <= b.frame) {
      const t = (frame - a.frame) / (b.frame - a.frame);
      return {
        x: lerp(a.x, b.x, t),
        y: lerp(a.y, b.y, t),
        opacity: lerp(a.visible ? 1 : 0, b.visible ? 1 : 0, t),
      };
    }
  }
  return { x: last.x, y: last.y, opacity: last.visible ? 1 : 0 };
}
```

- [ ] **Step 4: Eseguire i test (passano)**

Run: `npm test`
Expected: PASS (tutti i test di `carSpin.test.ts`).

- [ ] **Step 5: Commit**

```bash
git add src/lib/carSpin.ts src/lib/carSpin.test.ts
git commit -m "feat(car-360): config sequenza e funzioni pure (carSpin)"
```

---

### Task 6: `carSpots` — dati dei pallini + guardia d'integrità

**Files:**
- Create: `src/lib/carSpots.ts`
- Test: `src/lib/carSpots.test.ts`

**Interfaces:**
- Consumes: `SpotSample` da `carSpin.ts`; `treatments` da `treatments.ts`; `TreatmentId` da `carZones.ts`.
- Produces:
  - `interface CarSpot { id: TreatmentId; label: string; anchorFrame: number; samples: SpotSample[] }`
  - `carSpots: readonly CarSpot[]`

**Nota:** le coordinate `x/y` e gli `anchorFrame` sono valori **iniziali plausibili** da rifinire visivamente nel Task 9 ispezionando i fotogrammi reali. La struttura e la guardia sono ciò che va testato qui.

- [ ] **Step 1: Scrivere i test (falliscono)**

Create `src/lib/carSpots.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { carSpots } from "./carSpots";
import { treatments } from "./treatments";
import { SPIN } from "./carSpin";

describe("carSpots", () => {
  it("copre i 5 servizi mappati", () => {
    expect(carSpots.map((s) => s.id).sort()).toEqual(
      ["car-detailing", "centraline", "lucidatura", "restauro-pelle", "trattamento-vetri"].sort(),
    );
  });
  it("ogni id esiste in treatments.ts", () => {
    for (const s of carSpots) {
      expect(treatments.some((t) => t.id === s.id)).toBe(true);
    }
  });
  it("campioni ordinati e dentro il range dei frame", () => {
    for (const s of carSpots) {
      expect(s.samples.length).toBeGreaterThan(0);
      for (let i = 1; i < s.samples.length; i++) {
        expect(s.samples[i].frame).toBeGreaterThan(s.samples[i - 1].frame);
      }
      for (const smp of s.samples) {
        expect(smp.frame).toBeGreaterThanOrEqual(0);
        expect(smp.frame).toBeLessThanOrEqual(SPIN.frameCount - 1);
      }
      expect(s.anchorFrame).toBeGreaterThanOrEqual(0);
      expect(s.anchorFrame).toBeLessThanOrEqual(SPIN.frameCount - 1);
    }
  });
});
```

- [ ] **Step 2: Eseguire i test (falliscono)**

Run: `npm test`
Expected: FAIL — `carSpots` non esiste.

- [ ] **Step 3: Implementare `carSpots.ts`**

Create `src/lib/carSpots.ts`:

```ts
import type { SpotSample } from "./carSpin";
import { SPIN } from "./carSpin";
import type { TreatmentId } from "./carZones";
import { treatments } from "./treatments";

/**
 * Pallini dei servizi ancorati ai punti dell'auto lungo la rotazione. Ogni
 * servizio ha campioni {frame,x,y,visible} interpolati da resolveSpot. x/y in %
 * del box immagine. anchorFrame = fotogramma su cui la parte è meglio visibile
 * (usato da ZoneNav/pallini per portare l'auto lì). I valori sono iniziali e
 * vanno rifiniti visivamente sui fotogrammi reali (vedi piano, Task 9).
 */
export interface CarSpot {
  id: TreatmentId;
  label: string;
  anchorFrame: number;
  samples: SpotSample[];
}

// Valori di partenza: auto che ruota da front-3/4 (frame 0) a rear-3/4 (frame 35).
export const carSpots: readonly CarSpot[] = [
  {
    id: "centraline",
    label: "Motore",
    anchorFrame: 4,
    samples: [
      { frame: 0, x: 24, y: 58, visible: true },
      { frame: 8, x: 18, y: 58, visible: true },
      { frame: 16, x: 12, y: 58, visible: false },
    ],
  },
  {
    id: "trattamento-vetri",
    label: "Vetri",
    anchorFrame: 14,
    samples: [
      { frame: 4, x: 52, y: 34, visible: false },
      { frame: 12, x: 48, y: 32, visible: true },
      { frame: 24, x: 52, y: 32, visible: true },
      { frame: 32, x: 58, y: 34, visible: false },
    ],
  },
  {
    id: "restauro-pelle",
    label: "Interni",
    anchorFrame: 18,
    samples: [
      { frame: 8, x: 50, y: 46, visible: false },
      { frame: 16, x: 50, y: 46, visible: true },
      { frame: 26, x: 54, y: 46, visible: false },
    ],
  },
  {
    id: "lucidatura",
    label: "Carrozzeria",
    anchorFrame: 20,
    samples: [
      { frame: 6, x: 40, y: 62, visible: true },
      { frame: 20, x: 50, y: 62, visible: true },
      { frame: 34, x: 62, y: 62, visible: true },
    ],
  },
  {
    id: "car-detailing",
    label: "Detailing",
    anchorFrame: 28,
    samples: [
      { frame: 18, x: 66, y: 74, visible: false },
      { frame: 28, x: 70, y: 74, visible: true },
      { frame: 35, x: 78, y: 74, visible: true },
    ],
  },
];

// Guardia d'integrità a caricamento modulo (come carZones.ts): id esistente e
// campioni entro il range dei fotogrammi.
for (const s of carSpots) {
  if (!treatments.some((t) => t.id === s.id)) {
    throw new Error(`carSpots: id "${s.id}" non presente in treatments.ts`);
  }
  for (const smp of s.samples) {
    if (smp.frame < 0 || smp.frame > SPIN.frameCount - 1) {
      throw new Error(`carSpots: frame ${smp.frame} fuori range per "${s.id}"`);
    }
  }
}
```

- [ ] **Step 4: Eseguire i test (passano)**

Run: `npm test`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/lib/carSpots.ts src/lib/carSpots.test.ts
git commit -m "feat(car-360): dati dei pallini (carSpots) con guardia"
```

---

### Task 7: `Car360` — display fotogrammi + trascinamento

**Files:**
- Create: `src/components/sections/Car360.tsx`

**Interfaces:**
- Consumes: `SPIN`, `frameIndex` da `carSpin.ts`.
- Produces: componente `Car360` con props
  `{ frame: number; onFrameChange: (f: number) => void; showHint: boolean; onFirstDrag: () => void; children?: React.ReactNode }`.
  Rende lo stage con `aspectRatio` e i fotogrammi; gestisce il drag; sovrappone `children` (i pallini) e l'hint.

In questo task il componente è renderizzato ma NON ancora collegato a `CarExplorer` (integrazione nel Task 9). Verifica isolata via dev server montandolo temporaneamente non richiesta: la verifica reale avviene nel Task 9.

- [ ] **Step 1: Implementare `Car360.tsx`**

Create `src/components/sections/Car360.tsx`:

```tsx
"use client";

import { useRef, type PointerEvent, type ReactNode } from "react";
import Image from "next/image";
import { AnimatePresence, motion } from "framer-motion";
import { SPIN, frameIndex } from "@/lib/carSpin";

const CAR_ASPECT = "16 / 9";
const FADE_MASK = "linear-gradient(to bottom, #000 84%, transparent 99%)";
// Sensibilità: quanti pixel di trascinamento per avanzare di un fotogramma.
const PX_PER_FRAME = 18;

/**
 * Stage rotabile dell'auto: una sequenza di fotogrammi mostrata in base al
 * trascinamento orizzontale. Immobile da ferma (nessuna auto-rotazione).
 * I pallini dei servizi arrivano come `children` e vengono sovrapposti.
 */
export default function Car360({
  frame,
  onFrameChange,
  showHint,
  onFirstDrag,
  children,
}: {
  frame: number;
  onFrameChange: (f: number) => void;
  showHint: boolean;
  onFirstDrag: () => void;
  children?: ReactNode;
}) {
  const drag = useRef<{ startX: number; startFrame: number } | null>(null);
  const idx = frameIndex(frame);

  const onPointerDown = (e: PointerEvent<HTMLDivElement>) => {
    (e.currentTarget as HTMLDivElement).setPointerCapture(e.pointerId);
    drag.current = { startX: e.clientX, startFrame: frame };
    onFirstDrag();
  };
  const onPointerMove = (e: PointerEvent<HTMLDivElement>) => {
    if (!drag.current) return;
    const dx = e.clientX - drag.current.startX;
    onFrameChange(drag.current.startFrame + dx / PX_PER_FRAME);
  };
  const endDrag = (e: PointerEvent<HTMLDivElement>) => {
    if (drag.current) (e.currentTarget as HTMLDivElement).releasePointerCapture(e.pointerId);
    drag.current = null;
  };

  return (
    <div
      className="relative w-full cursor-grab touch-none select-none active:cursor-grabbing"
      style={{ aspectRatio: CAR_ASPECT }}
      onPointerDown={onPointerDown}
      onPointerMove={onPointerMove}
      onPointerUp={endDrag}
      onPointerCancel={endDrag}
    >
      <div
        aria-hidden
        className="pointer-events-none absolute inset-x-[8%] bottom-[5%] h-[12%] rounded-[50%] bg-black/70 blur-2xl"
      />

      {/* Tutti i fotogrammi montati; solo quello attivo è visibile. Il primo ha
          priority per comparire subito; gli altri fanno da precaricamento. */}
      {Array.from({ length: SPIN.frameCount }).map((_, i) => (
        <Image
          key={i}
          src={SPIN.srcFor(i)}
          alt={i === 0 ? "Audi nera Garden's Cars, vista tre quarti anteriore" : ""}
          aria-hidden={i !== 0}
          fill
          priority={i === 0}
          sizes="(max-width: 1024px) 100vw, 60vw"
          className="pointer-events-none object-contain"
          style={{
            opacity: i === idx ? 1 : 0,
            maskImage: FADE_MASK,
            WebkitMaskImage: FADE_MASK,
          }}
        />
      ))}

      {children}

      <AnimatePresence>
        {showHint && (
          <motion.div
            aria-hidden
            initial={{ opacity: 0, y: 6 }}
            animate={{ opacity: 1, y: 0 }}
            exit={{ opacity: 0 }}
            style={{ borderColor: "rgba(245,244,240,0.15)" }}
            className="pointer-events-none absolute bottom-2 left-1/2 -translate-x-1/2 rounded-full border bg-ink/70 px-4 py-2 font-display text-xs uppercase tracking-widest text-paper/70 backdrop-blur"
          >
            Trascina per ruotare
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
}
```

- [ ] **Step 2: Verifica compilazione**

Run: `npx tsc --noEmit`
Expected: nessun errore relativo a `Car360.tsx`.

- [ ] **Step 3: Commit**

```bash
git add src/components/sections/Car360.tsx
git commit -m "feat(car-360): stage rotabile con trascinamento"
```

---

### Task 8: Overlay pallini dei servizi

**Files:**
- Create: `src/components/sections/CarSpots.tsx`

**Interfaces:**
- Consumes: `carSpots` da `carSpots.ts`; `resolveSpot` da `carSpin.ts`; `treatments`.
- Produces: componente `CarSpots` con props
  `{ frame: number; activeId: string; onPreview: (id: TreatmentId) => void; onEndPreview: () => void; reduce: boolean | null }`.
  Rende un `Link` per ogni pallino visibile, posizionato via `resolveSpot`.

- [ ] **Step 1: Implementare `CarSpots.tsx`**

Create `src/components/sections/CarSpots.tsx`:

```tsx
"use client";

import Link from "next/link";
import { motion } from "framer-motion";
import { carSpots } from "@/lib/carSpots";
import { resolveSpot } from "@/lib/carSpin";
import { treatments } from "@/lib/treatments";
import type { TreatmentId } from "@/lib/carZones";

/**
 * Pallini dei servizi sovrapposti all'auto: ognuno segue la rotazione
 * (posizione/opacità da resolveSpot) e linka alla sezione del trattamento.
 * I pallini con opacità ~0 sono non interattivi.
 */
export default function CarSpots({
  frame,
  activeId,
  onPreview,
  onEndPreview,
  reduce,
}: {
  frame: number;
  activeId: string;
  onPreview: (id: TreatmentId) => void;
  onEndPreview: () => void;
  reduce: boolean | null;
}) {
  return (
    <>
      {carSpots.map((spot) => {
        const { x, y, opacity } = resolveSpot(spot.samples, frame);
        const hidden = opacity < 0.05;
        const t = treatments.find((tr) => tr.id === spot.id);
        const isActive = spot.id === activeId;
        return (
          <Link
            key={spot.id}
            href={`/trattamenti#${spot.id}`}
            onMouseEnter={() => onPreview(spot.id)}
            onFocus={() => onPreview(spot.id)}
            onMouseLeave={onEndPreview}
            aria-label={`Vai al servizio ${t?.title ?? spot.label}`}
            className="absolute -translate-x-1/2 -translate-y-1/2 rounded-full"
            style={{
              left: `${x}%`,
              top: `${y}%`,
              opacity,
              pointerEvents: hidden ? "none" : "auto",
              transition: reduce ? undefined : "opacity 200ms ease",
            }}
            tabIndex={hidden ? -1 : 0}
            aria-hidden={hidden}
          >
            <span
              aria-hidden
              className="absolute left-1/2 top-1/2 h-3.5 w-3.5 -translate-x-1/2 -translate-y-1/2 rounded-full bg-racing-bright shadow-[0_0_0_4px_rgba(46,139,67,0.25)]"
            />
            {!reduce && isActive && !hidden && (
              <motion.span
                aria-hidden
                className="absolute left-1/2 top-1/2 h-3.5 w-3.5 -translate-x-1/2 -translate-y-1/2 rounded-full bg-racing-bright/60"
                animate={{ scale: [1, 2.6], opacity: [0.6, 0] }}
                transition={{ duration: 1.4, repeat: Infinity, ease: "easeOut" }}
              />
            )}
          </Link>
        );
      })}
    </>
  );
}
```

- [ ] **Step 2: Verifica compilazione**

Run: `npx tsc --noEmit`
Expected: nessun errore relativo a `CarSpots.tsx`.

- [ ] **Step 3: Commit**

```bash
git add src/components/sections/CarSpots.tsx
git commit -m "feat(car-360): overlay pallini interpolati"
```

---

### Task 9: Integrazione in `CarExplorer` + rifinitura pallini

**Files:**
- Modify: `src/components/sections/CarExplorer.tsx`

**Interfaces:**
- Consumes: `Car360`, `CarSpots`, `carSpots` (per `anchorFrame`), `frameIndex`.
- Rimpiazza l'uso di `CarStage` con `Car360` + `CarSpots`. `ZoneNav` e `ServicePanel` restano.

- [ ] **Step 1: Ricablare `CarExplorer`**

In `src/components/sections/CarExplorer.tsx`:
- Rimuovere l'import e l'uso di `CarStage` e la sua definizione (la funzione `CarStage` e gli import ora inutilizzati: `Image`, `carZones`, `DEFAULT_ZONE_ID`).
- Importare: `import Car360 from "./Car360";`, `import CarSpots from "./CarSpots";`, `import { carSpots } from "@/lib/carSpots";`, `import { SPIN } from "@/lib/carSpin";`.
- Sostituire lo stato/preview con quello basato sul frame. La versione della funzione principale diventa:

```tsx
export default function CarExplorer() {
  const reduce = useReducedMotion();
  const [frame, setFrame] = useState<number>(carSpots[2].anchorFrame); // Carrozzeria
  const [activeId, setActiveId] = useState<string>(carSpots[2].id);
  const [touched, setTouched] = useState(false);

  const active = treatmentById(activeId);

  // hover/focus su una zona: evidenzia il servizio e porta l'auto all'angolo
  // dove quella parte è meglio visibile (anchorFrame).
  const preview = (id: string) => {
    setActiveId(id);
    const spot = carSpots.find((s) => s.id === id);
    if (spot) setFrame(spot.anchorFrame);
    setTouched(true);
  };
  const endPreview = () => {};

  return (
    <section
      id="esplora"
      className="relative overflow-hidden bg-ink py-24 text-paper md:py-32"
    >
      <div className="glow-racing pointer-events-none absolute left-1/2 top-0 h-[40rem] w-[40rem] -translate-x-1/2 opacity-20 blur-3xl" />

      <div className="wrap relative">
        <div className="max-w-2xl">
          <Reveal>
            <span
              className="eyebrow text-racing-bright"
              style={{ fontSize: "clamp(1rem, 1.3vw, 1.25rem)" }}
            >
              Esplora i servizi
            </span>
          </Reveal>
          <Reveal delay={0.05}>
            <h2 className="display-xl mt-5">Un&apos;auto, cinque trattamenti.</h2>
          </Reveal>
          <Reveal delay={0.1}>
            <p className="mt-5 max-w-md text-paper/70">
              Trascina per ruotare l&apos;auto e vai dritto al trattamento che ti
              interessa.
            </p>
          </Reveal>
        </div>

        <div className="mt-12 grid items-center gap-8 lg:mt-16 lg:grid-cols-[1.4fr_1fr] lg:gap-12">
          <Car360
            frame={frame}
            onFrameChange={setFrame}
            showHint={!touched}
            onFirstDrag={() => setTouched(true)}
          >
            <CarSpots
              frame={frame}
              activeId={activeId}
              onPreview={preview}
              onEndPreview={endPreview}
              reduce={reduce}
            />
          </Car360>
          <ServicePanel treatment={active} reduce={reduce} />
        </div>

        <ZoneNav
          activeId={activeId}
          onPreview={preview}
          onEndPreview={endPreview}
        />
      </div>
    </section>
  );
}
```

- Aggiornare `ZoneNav` internamente: usa già `carZones` per elencare le voci; sostituire la sorgente con `carSpots` (stesso shape `{id,label}`), cambiando l'import in cima da `carZones` a `carSpots` e `carZones.map` → `carSpots.map`. Rimuovere `DEFAULT_ZONE_ID` se non più usato.

- [ ] **Step 2: Verifica compilazione + lint**

Run: `npx tsc --noEmit && npm run lint`
Expected: nessun errore; nessun import inutilizzato (`Image`, `carZones`, `DEFAULT_ZONE_ID`, `FocusEvent` se non più usati vanno rimossi).

- [ ] **Step 3: Verifica visiva sul dev server**

Avviare `npm run dev` (nota: il progetto può girare su :3001 se :3000 è occupato — verificare l'output). Con Playwright (`animations:'disabled'` per gli screenshot) aprire la home, sezione `#esplora`:
- L'auto è immobile e mostra un fotogramma iniziale (Carrozzeria).
- Trascinando, l'auto ruota fluidamente su ~180°; l'hint "Trascina per ruotare" sparisce dopo il primo drag.
- I pallini compaiono/sfumano seguendo la rotazione.

- [ ] **Step 4: Rifinire i campioni dei pallini**

Confrontando gli screenshot con le posizioni reali delle parti, aggiornare `x/y/frame/visible` e `anchorFrame` in `src/lib/carSpots.ts` finché ogni pallino sta sul punto giusto e compare all'angolo corretto. Ripetere Step 3–4 finché il risultato è corretto. Rieseguire `npm test` per assicurarsi che la guardia/ i test passino ancora.

- [ ] **Step 5: Commit**

```bash
git add src/components/sections/CarExplorer.tsx src/lib/carSpots.ts
git commit -m "feat(car-360): integra spin e pallini in CarExplorer"
```

---

### Task 10: Fallback immagini + pulizia `carZones`

**Files:**
- Modify: `src/components/sections/Car360.tsx`
- Delete: `src/lib/carZones.ts` (se non più referenziato) — oppure conservarne solo `TreatmentId`.

**Interfaces:**
- `TreatmentId` deve restare disponibile: se `carZones.ts` viene rimosso, spostare `export type TreatmentId` in `carSpots.ts` e aggiornare gli import in `CarSpots.tsx`.

- [ ] **Step 1: Gestire l'errore di caricamento fotogrammi**

In `Car360.tsx`, aggiungere uno stato `failed` e, su `onError` dell'`Image` del frame 0, impostarlo; quando `failed` è vero mostrare comunque il frame 0 come immagine statica (già montato) senza bloccare i pallini. Codice: aggiungere `const [failed, setFailed] = useState(false);` (import `useState`) e sul primo `Image` `onError={() => setFailed(true)}`; avvolgere l'hint drag in `!failed &&` così non invita a trascinare se la sequenza non c'è.

- [ ] **Step 2: Verificare i riferimenti a `carZones`**

Run: `grep -rn "carZones\|DEFAULT_ZONE_ID" src`
Expected: nessun uso residuo tranne (eventualmente) il tipo `TreatmentId`.

- [ ] **Step 3: Consolidare `TreatmentId`**

Se l'unico uso di `carZones.ts` è `TreatmentId`, spostare la definizione del tipo in `carSpots.ts`, aggiornare l'import in `CarSpots.tsx`, ed eliminare `src/lib/carZones.ts` e `src/lib/carZones` di test se presenti. Altrimenti lasciare `carZones.ts` col solo `TreatmentId`.

- [ ] **Step 4: Verifica finale**

Run: `npx tsc --noEmit && npm run lint && npm test`
Expected: tutto verde.

- [ ] **Step 5: Verifica visiva finale + reduced-motion**

Con Playwright: ricontrollare la sezione su desktop e mobile (viewport ridotto). Emulare `prefers-reduced-motion: reduce` e verificare che i pallini non pulsino e le transizioni siano attenuate, ma l'auto sia ancora trascinabile.

- [ ] **Step 6: Commit**

```bash
git add -A
git commit -m "feat(car-360): fallback immagini e pulizia carZones"
```

---

## Self-Review

**Spec coverage:**
- Approccio a fotogrammi → Task 1–3, 7.
- Arco 180° + estendibilità 360° → `SPIN` (Task 5), agnostico a frameCount/wrap.
- Componente `Car360` che sostituisce `CarStage`, riuso `ServicePanel`/`ZoneNav` → Task 7, 9.
- Modello dati pallini (samples interpolati, visible) → Task 5–6, 8.
- Mappatura 5 servizi → Task 6.
- Interazione drag + immobile da ferma + hint → Task 7, 9.
- ZoneNav/anchorFrame → Task 9.
- Accessibilità (reduced-motion, tastiera, fallback) → Task 8, 10.
- Performance (primo frame priority, precaricamento, WebP) → Task 3, 7.
- Pipeline asset (base pulita, orbita, estrazione) → Task 1–3.

**Placeholder scan:** nessun "TBD"/"gestire come opportuno". I valori di `carSpots` sono dichiarati "iniziali da rifinire" con un task esplicito (9.4) che lo fa — non è un placeholder ma un passo di calibrazione visiva previsto.

**Type consistency:** `SpotSample`/`ResolvedSpot`/`resolveSpot` definiti in Task 5 e usati coerentemente in Task 6/8; `CarSpot`/`carSpots` in Task 6 usati in Task 8/9; props di `Car360`/`CarSpots` coerenti con l'uso in Task 9; `TreatmentId` tracciato in Task 10.
